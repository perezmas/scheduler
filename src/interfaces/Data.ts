/**This is extended by all the other data interfaces, which are used to represent components internally.*/
interface AbstractData {
    /**A globally unique identifier that identifies this object. */
    uuid: string;
}

export default AbstractData;
